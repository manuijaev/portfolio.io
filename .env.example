# Admin credentials are now stored locally in the browser.
# These are kept for reference only - the app uses local storage by default.
ADMIN_EMAIL=Kenyaniemmanuel44@gmail.com
ADMIN_PASSWORD=admin123
